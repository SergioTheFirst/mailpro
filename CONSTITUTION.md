ОБНОВЛЁННАЯ ФИНАЛЬНАЯ КОНСТИТУЦИЯ MailBot Premium v26 (2025-2026)

CONSTITUTION

 I. Введение

MailBot Premium v26 - высоконадёжная система анализа писем и вложений, работающая даже на слабых Windows-машинах (Celeron, 3-4 ГБ RAM) и выдающая минимальные, точные, проверенные факты в формате <=200 символов.

Этот документ определяет абсолютные правила проекта, обязательные для:

архитектуры

реализации кода

Codex-генерации

CI-проверок

дальнейших версий

 II. Нерушимые Принципы («Core Invariants»)
1. Качество важнее скорости

MailBot может работать медленно (минуты), но результат должен быть идеален.

2. Zero Water

Запрещены любые лишние слова, комментарии, объяснения.
Вывод содержит только факты.

3. Silence on Absence

Если факта нет -> ничего не писать.
Запрещено выводить:
«нет данных», «не найдено», «отсутствует», «none», пустые блоки.

4. Полная проверяемость

Каждый факт в summary ДОЛЖЕН находиться в исходном тексте.

5. Длина итогового сообщения <= 200 символов

Никогда не превышать.

6. Бот обязан работать ВСЕГДА

Даже если:

RAM низкая

OCR не доступен

ML нельзя загрузить

PDF битый

IMAP глючит

🏛️ III. Философия Архитектуры Premium v26

Один монолитный цикл (start.py).

Worker-процесс = чистая логика обработки.

Каждый шаг pipeline выполняется всегда, но подбирает доступный метод (гибридные ветки).

Принцип «не падай» важнее, чем идеальность.

При недостатке RAM -> автоматический переход в Guaranteed Mode.

🧵 IV. Pipeline (непрерывная цепочка)
1. IMAP Hybrid Fetch (UID>last + SINCE last_check)
2. Лёгкое текстовое извлечение (PDF, DOCX, XLSX)
3. OCR (EasyOCR) - только при достаточной RAM
4. Классификация (keyword -> ML -> LLM)
5. Извлечение фактов (LLM)
6. Верификация фактов (LLM)
7. Локальная валидация:
      - NONE cleanup
      - numbers check
      - date check
      - negation detection
      - Jaccard threshold
8. Форматирование (<=200 chars)
9. Telegram delivery


Эта цепочка НЕ может быть изменена или укорочена.

 V. Guaranteed Mode (Гарантированный режим)

Активируется автоматически, если:

RAM свободно < 1.2 ГБ

Torch/EasyOCR невозможно загрузить

Классификация ML недоступна

В этом режиме:

PDF -> pypdf/pikepdf

DOCX -> python-docx / docx2txt

XLSX -> pandas + openpyxl(read_only=True)

NO OCR

Классификация keyword-only

LLM extract + verify

Формат <=200 chars

Guaranteed Mode - не деградация, а обязательный безопасный режим.

🟧 VI. Адаптивные правила производительности
✔ OCR включается если:
RAM >= 800 МБ свободно


Иначе OCR -> OFF.

✔ ML-классификация включается если:
RAM >= 2 ГБ свободно


Иначе ML -> OFF.

✔ Action Engine - обязательный первый слой

Если уверенность >=0.9 -> LLM не вызывается.
Если <0.9 -> обычный LLM pipeline.

🟥 VII. Запрещено навсегда
ERROR 1. Adaptive Extractor / микро-модели по отправителю

Причина: рост state.json до сотен МБ -> OOM.

ERROR 2. Самописный XLSX-stream parser

Причина: sharedStrings.xml убивает RAM.

ERROR 3. Самодельный OCR без ML

Причина: низкая точность, долгий runtime.

ERROR 4. Token-by-token LLM guard

Причина: убивает 35-60% корректных фактов.

ERROR 5. Любые DLL/EXE инструменты извлечения текста

Т.е. запрещены:

catdoc.exe

xlhtml.exe

pdftotext.exe

poppler

Только чистые Python-библиотеки.

ERROR 6. Модели >100 МБ, spaCy, Stanza, BERT-тяжёлые

Категорически запрещены.

ERROR 7. Любые формы "нет данных"

Даже косвенные.

🟦 VIII. Разрешённые и рекомендованные технологии
✔ PDF
pypdf + pikepdf  (DLL-free)

✔ DOC / DOCX
docx2txt + python-docx

✔ EXCEL
pandas.read_excel(..., engine="openpyxl", read_only=True)

✔ OCR
easyocr + torch(cpu-only)


Но только при RAM >=800 МБ.

✔ LLM

Cloudflare Workers AI (Llama-3-8B-Instruct).
Только extract/verify.
Никаких генеративных текстов.

🧱 IX. State-менеджмент: динамический лимит размера
Лимит зависит от RAM машины:
RAM машины	Максимальный размер state.json
RAM < 4 ГБ	100 МБ
RAM >= 8 ГБ	200-300 МБ
Правило очистки:

Если state.json превышает лимит ->
-> оставляем последние 3000 записей, остальное удаляется.

Никакие адаптивные модели не имеют права расти безгранично.

📜 X. Правила LLM

Всегда добавлять в промпт:

ОСНОВЫВАЙСЯ ТОЛЬКО НА ТЕКСТЕ. НЕ ФАНТАЗИРУЙ.


Формат ответа - одна строка с |.

Если нет фактов - NONE.

Любой ответ >200 символов -> сжать.

Выделять факты только явные (или числовые реформулировки).

🧹 XI. Локальная валидация фактов

Валидация обязательна.

NONE-clean

Number-check

Date-check

Negation check

Jaccard >= 0.35 / 0.5 / 0.6

Итоговое summary <=200 символов

Если что-то не прошло валидацию -> silence.

🎨 XII. Формат вывода

Используются:

SUM сумма | DATE дата | №док | ACTION действие


Каждый блок обязателен только если факт есть.

🛠 XIII. Обязанности Codex

Codex обязан:

Читать Конституцию перед каждым шагом.

Подтверждать соответствие кода Конституции.

Самостоятельно выполнять self-test.

Не использовать запрещённые библиотеки.

Строго соблюдать формат summary.

Уважать Guaranteed Mode.

Генерировать только RAM-safe, Windows-safe код.

🧩 XIV. Поправки и обновления

Все изменения Конституции идут:

с архитектурным обоснованием,

с diff/patch для репозитория,

с записью в CHANGELOG.md.

🏁 XV. Завершение

MailBot Premium v26 строится на принципах:

надёжности,

предсказуемости,

отсутствия фантазий,

минимализма,

контроля фактов,

гарантированного выполнения.

Эта Конституция - основной закон проекта.

Любой код, не соответствующий Конституции, считается ошибочным и подлежит исправлению немедленно.