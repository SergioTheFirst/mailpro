Живая Конституция Проекта

---

## 📌 **Введение**

MailBot Premium v26 — это система сверхвысокой надёжности, работающая на слабом оборудовании (3GB RAM, Windows, Celeron) и обязующаяся выдавать **краткие, точные и полностью проверенные факты** из писем и вложений.

Эта Конституция определяет:

* философию проекта,
* нерушимые инварианты,
* разрешённые и запрещённые подходы,
* структуру многоступенчатого pipeline,
* правила LLM, OCR, классификации, валидации, форматирования,
* нормы поведения системы в условиях ограниченных ресурсов,
* обязанности Codex при генерации кода.

Конституция — **главный источник истины**.
Все изменения кода должны соответствовать Конституции.

---

# 🧠 **I. Нерушимые Принципы (Core Invariants)**

Эти правила **абсолютны** и никогда не могут быть нарушены.

### **1. Качество важнее скорости**

MailBot может обрабатывать письмо 1–3 минуты.
Но результат обязан быть максимально точным.

### **2. Zero Water**

В выводе запрещено:

* любые лишние слова,
* вступительные фразы,
* “по предоставленному тексту…",
* объяснения отсутствия данных,
* перечисление пустых элементов.

Вывод содержит **только факты**, ничего лишнего.

### **3. Silence on Absence**

Если факт отсутствует в тексте —
**нам лучше молчать, чем писать “нет данных”**.

Нельзя выводить:

* “нет данных”
* “не указано”
* “не найдено”
* “отсутствует”

### **4. Точность фактов = строгая обязательность**

Любой факт в итоговом сообщении:

* должен быть **найден в исходном тексте** либо
* быть **однозначной реформулировкой** (например “сто тысяч” → “100000”).

### **5. Итоговое сообщение ≤ 200 символов**

Формат обязан быть:

* компактным,
* структурированным,
* выраженным через значимые символы (|, эмодзи),
* понятным за 1 секунду чтения.

### **6. Система обязана отработать ВСЕГДА**

MailBot никогда не имеет права:

* упасть,
* ждать ресурсов,
* откладывать обработку,
* зависать на OCR, ML или тяжёлом анализе.

---

# ⚙️ **II. Архитектурная Философия Premium v26**

Система проектируется как:

* **всегда работающее ядро (Guaranteed Mode)**,
* со ступенчатым повышением качества,
* но без риска OOM, зависаний, тайм-аутов.

Бот должен работать **от 100% до 10% ресурсов**, не ломаясь.

---

# 🧱 **III. Этапы Pipeline (непрерывные, обязательные)**

Pipeline ВСЕГДА состоит из следующей цепочки (без пропусков этапов):

```
1. IMAP Hybrid Fetch (UID + SINCE)
2. Лёгкое извлечение текста (pdfminer, mammoth, catdoc)
3. Условный OCR (если ресурсы позволяют)
4. Классификация документа:
      a) Keyword классификация (обязательно)
      b) ML-классификация (если позволяет RAM)
      c) LLM-классификация (если предыдущие не уверены)
5. Извлечение фактов через LLM
6. Верификация фактов через LLM
7. Локальная проверка фактов:
      - NONE-чистка
      - числовая проверка
      - проверка дат
      - проверка отрицаний
      - BLEU-lite / Jaccard сравнение
8. Форматирование вывода ≤ 200 символов
9. Отправка в Telegram
```

Этапы 1–9 всегда выполняются.
Разница — только в **методе внутри этапа** (ветвление).

---

# 🟦 **IV. Условные (Adaptive) Механизмы**

## 1. OCR включается только если:

* свободно > **1.5 GB RAM**,
* pdfminer дал пустой текст,
* документ классифицирован как “scan”.

В противном случае:
→ OCR отключён навсегда для данного письма
→ это не ошибка
→ pipeline продолжается.

## 2. ML-классификация включается только если:

* keyword-классификация неуверенна,
* свободно > **2 GB RAM**,
* ML загрузка не вызовет OOM.

Если условия не выполнены:
→ ML отключена
→ классификация возвращается к keyword+LLM.

## 3. Местная LLM (ruT5, transformers) запрещена

За исключением **micro-fallback** на текст <500 символов.

## 4. spaCy и любые модели >100MB полностью запрещены

## 5. Любой тяжёлый шаг может быть отменён

Но pipeline не должен ломаться.

---

# 🔍 **V. Правила LLM**

### **1. LLM не имеет права фантазировать**

В каждом промпте обязательно:

```
ОСНОВЫВАЙСЯ ТОЛЬКО НА ПРЕДОСТАВЛЕННОМ ТЕКСТЕ.
НЕ ДОДУМЫВАЙ.
НЕ ФАНТАЗИРУЙ.
ЕСЛИ ФАКТА НЕТ — НЕ ПИШИ ПРО НЕГО.
```

### **2. Формат ответа на факты — строго одной строкой**

Используем разделитель `|`.

Пример:

```
СУММА: 150000 RUB | СРОК: 20.12.2024 | №123/7
```

### **3. Если нет фактов, LLM возвращает:**

```
NONE
```

*(но пользователю это не выводится)*

### **4. Любой превышающий 200 символов ответ

должен быть рекурсивно сжат LLM.**

---

# 🧹 **VI. Правила Очистки и Валидации**

MailBot обязан:

### 1. Удалять любые сегменты, содержащие NONE

Пока не останутся только факты.

### 2. Проверять каждую сумму в исходном тексте

Если число отсутствует → факт удаляется.

### 3. Проверять каждую дату

Если нет в тексте → факт удаляется.

### 4. Проверять отрицания

Если письмо содержит "не оплатить", "отказ", "высказать возражение" →
алгоритм обязан скорректировать значение факта.

### 5. Применить BLEU-lite или Jaccard:

* короткие summary: порог 0.35
* средние: 0.5
* длинные: 0.6

Если ниже порога:
→ summary отбрасывается
→ заменяется fallback summary
→ если fallback пуст → silence

---

# 🟫 **VII. Форматирование Итогового Сообщения**

Итог:

* максимум 200 символов,
* только факты,
* без вводных фраз,
* компактно,
* читабельно.

Пример:

```
💰150000₽ | 📅20.12 | №123/7 | ▶ оплатить
```

---

# 🟥 **VIII. Guaranteed Mode (Гарантированный Режим)**

**Новый раздел, добавленный после критического анализа.**

MailBot обязан иметь режим, который работает **при любых ресурсах**, даже на:

* постоянно низкой RAM (< 1GB свободно),
* забитом CPU,
* отсутствии OCR,
* невозможности ML-классификации,
* плохих PDF.

В этом режиме используются:

* pdfminer/mammoth/catdoc,
* keyword-классификация,
* Cloudflare LLM extract,
* Cloudflare LLM verify,
* local regex/date/number checks,
* short formatting.

Если ни один факт не подтверждён:
→ silence (только тема письма).

**Guaranteed Mode НЕ является деградацией.
Это — штатный режим надёжности.**

---

# 🟧 **IX. Запрещено НАВСЕГДА**

MailBot не имеет права:

* использовать spaCy, Stanza, BERT-size модели, большие seq2seq,
* использовать локальный ruT5 на текстах >500 символов,
* выполнять OCR без проверки RAM,
* падать из-за OOM,
* записывать “нет данных”,
* выводить пустые блоки,
* генерировать больше 200 символов,
* пропускать этапы pipeline,
* зависать в операциях.

---

# 🟩 **X. Обязанности Codex (генератора кода)**

Codex должен:

1. Строго следовать Конституции.
2. Использовать только разрешённые библиотеки.
3. Уважать Guaranteed Mode.
4. Строить pipeline с ветвлениями, но без пропусков.
5. Генерировать код, устойчивый к OOM и Windows.
6. Никогда не выводить текст о пустых данных.
7. Всегда ограничивать summary ≤200 символов.
8. Применять валидацию фактов на локальной стороне.
9. Соблюдать структуру проекта Premium v26.
10. Разрабатывать под философию “Slow & Perfect”.

---

# 🟦 **XI. Изменения Конституции**

Конституция — живой инженерный документ.
Поправки могут вноситься только:

* после анализа архитектурной команды,
* если изменение улучшает надёжность, предотвращает OOM или снижает галлюцинации.

Каждое изменение фиксируется в CHANGELOG.md.

---

# 🏁 **Заключение**

MailBot Premium v26 существует ради:

* стабильности,
* точности,
* бескомпромиссной надёжности,
* абсолютного контроля фактов.

Эта Конституция — его фундамент.
Любой код, который не соответствует ей, считается ошибочным и должен быть исправлен.

